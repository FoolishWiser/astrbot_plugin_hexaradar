# Changelog

本项目的所有重要变更都会记录在此文件中。

格式基于 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.0.0/)，版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

## [v3.0.3] - 2026-08-06

### 变更

- 徽章改为三角布局：个人基准分综合分保持在姓名行（上），社会参考分与稀缺值移至下方独立一行（卡片/雷达视图右下、列表姓名下方），开启全部开关后个人信息（姓名/备注）不再被挤压局促

## [v3.0.2] - 2026-08-06

### 修复

- 修复稀缺值失效：`state` 缺少 `scarcityEnabled` 且 `loadList` 未同步该标志，导致首页徽章缺失、详情页稀缺值切换无反应（点击立即回落）

### 变更

- 设置集成进主 WebUI：标题「六边形能力雷达」右侧新增透明齿轮图标（⚙，悬停变主题色并显示"设置"提示），点击弹出页内设置弹窗（显示开关/密码/匹配别名），保存即时生效

## [v3.0.1] - 2026-08-06

### 修复

- 修复页面错误「scarcityBadge is not defined」：稀缺值徽章函数定义缺失（发布时仅保留调用处），已补全

## [v3.0.0] - 2026-08-06

### 新增

- **稀缺值（独特性算法）**：以同龄均值 50 为基准衡量能力组合罕见程度——偏差 L、全面性 cosθ、年龄参数 β、归一化基准 U_ref，公式 min(100, U_raw/U_ref×100)；独立开关 `show_scarcity_score`；首页橙色「稀 xx」胶囊；详情页/只读弹窗切换扩为三段（个人基准分 | 社会参考分 | 稀缺值，附 β/U_ref 说明）；排序下拉「按稀缺值」；`get_scarcity_score` LLM 工具
- **LLM 自动评审改分**：`on_llm_response` 钩子，回复文本命中库内姓名（含别名）时自动发起评审调用（仅输出紧凑 JSON 提案），静默应用改分并以箭头表格+加粗理由发送改分提示（模板生成，零额外 LLM token）；`auto_review` 开关（默认开启）；防递归保护
- **更新记录**：每人滚动保留 10 条（时间/来源/批次/变更明细 from→to），AI 同一次回答（同一 message_id）内多次改分合并为一条大记录；详情页新增纯图标按钮 🕘 弹出该人历史面板
- **设置页面**（新 Page `pages/settings/`）：全部配置可调整（密码、社会分/稀缺值/自动改分开关），显示开关实时生效无需重载插件；**匹配别名编辑**——库内姓名可自定义别名，自动评审匹配对话时匹配别名而非库内名
- 聊天指令：`/radar rank` 支持 `稀缺` 维度

## [v2.0.0] - 2026-08-05

### 新增

- **社会参考分**（25 岁成熟基准算法）：基于六项评分与年龄计算——25 岁基准权重（判断 3.0/心理 2.5/社交 2.5/认知 2.0/方向 1.5/学习 1.0）+ 年龄系数分段线性插值（10/16/20/25/30/40 岁节点），公式 Σ(W·c·S)÷Σ(W·c)；原六项分数更名为「个人基准分」
- 人物新增**年龄**字段（可填项，新建/编辑均可填写，可清空）；无年龄的人员不显示社会参考分且不参与社会分排序
- 配置开关 `show_social_score`：关闭时全部社会参考分隐藏
- 首页（卡片/列表/雷达三视图）：社会参考分仅显示综合分，与个人基准分综合分同层级并排展示（青色胶囊）
- 详情页与只读弹窗：顶部「个人基准分 | 社会参考分」切换（仅开关开启且有年龄时出现）；切换仅改变综合分显示，小项分数、雷达图、评分理由均不变；社会模式附年龄系数说明
- 新增**雷达图视图**（分段控件第三按钮）：网格展示每人雷达图+姓名+双综合分，点击进详情，支持排序/搜索/拼音筛选
- 排序下拉新增「按社会参考分」；社会参考分的 6 个小项不参与排序
- AI 工具：`set_radar_scores` 增加可选 `age`，`get_radar_scores` 返回年龄与社会参考分

## [v1.2.0] - 2026-08-05

### 变更

- 列表模式操作按钮延续胶囊风格：编辑为紫色调胶囊（主题底+描边），删除为危险红调胶囊，悬停实色反白并带光晕
- 拼音索引栏「隐形」设计：默认透明淡显（仅 35% 不透明度、无边框背景），悬停或筛选激活时平滑浮现完整样式
- 「筛选重置」按钮恒定占位：出现/消失不再导致拼音栏位移，仅通过透明度与可见性切换

## [v1.1.0] - 2026-08-05

### 变更

- 列表模式仅保留「姓名」「操作」两列：六项小分不再常驻展示，仅在按对应小项排序时于综合分旁展示该小项一条分数（次级胶囊样式）
- 表格固定布局铺满全宽，所有行横向宽度统一，总宽度保持不变；超长备注末尾省略号截断
- 操作按钮与姓名/分数行对齐至同一高度
- 滚动条样式统一美化（细条圆角滑块、透明轨道、悬停主题色），兼容 WebKit 与 Firefox

### 修复

- 弹窗内容超出视口时可内部滚动（max-height 92vh + overflow-y）
- 弹窗与滑块区每次打开时滚动位置归顶

## [v1.0.0] - 2026-08-05

### 变更

- 图标模式：点击人物卡片默认打开**只读详情弹窗**（雷达图 + 六项评分明细与逐项理由 + 备注），底部「编辑」按钮切换进入编辑模式，「关闭」退出
- 列表模式：综合分胶囊常驻显示于角色姓名旁（与图标模式一致）；按小项排序时在综合分旁同步显示该小项分数（次级胶囊样式，虚线描边，主次分明）
- 拼音索引栏：点击字母由「滚动定位」改为「筛选」该首字母角色；再次点击同一字母或点击「✕」按钮清除筛选，恢复完整列表
- 拼音索引栏宽度固定 30px，总长度不再随内容变化；「筛选重置」按钮独立于拼音栏下方（同宽度同设计），仅筛选激活时出现
- 图标模式：按小项排序时，置顶小项的分数值改为彩色胶囊展示
- 移除列表「综合/排序项」独立列（综合分与小项分数统一并入姓名旁胶囊展示）

### 修复

- 列表姓名行综合分胶囊被 `.badge` 的 `margin-left: auto` 推到单元格最右，改为紧邻姓名

## [v0.3.0] - 2026-08-05

### 新增

- 列表模式右侧固定拼音导航栏（A-Z 通讯录式交互，点击滚动到对应首字母条目，无此字母时定位到下一个可用字母）
- 列表模式新增「排序项」列：按某小项排序时，在综合分旁同步高亮显示该小项分数；综合分每行始终展示
- 删除确认弹窗（修复沙箱 iframe 中 `confirm()` 静默失效导致删除不可用的问题）

### 变更

- 默认排序规则：仅「姓名」默认升序（A-Z），其余数值类指标一律默认降序
- 图标模式排序视觉反馈：按小项排序时，该小项进度条置顶显示并高亮（带升/降箭头）

## [v0.2.0] - 2026-08-05

### 新增

- 详情页：点击卡片头像或列表姓名跳转（hash 路由 `#/person/<名字>`），左侧大尺寸雷达图，右侧逐项评分明细、评分理由与整体备注
- 逐项评价理由（`reasons` 字段）：WebUI 编辑弹窗逐项填写，`set_radar_scores` 工具可选传入，旧数据自动兼容
- AI 新工具：`search_radar_persons`（拼音/同音模糊搜索）、`get_radar_ranking`（按综合分或任一维度排行）
- 聊天指令：`/radar rank [维度] [topN]` 单项排行、`/radar search <关键词>` 模糊搜索；`/radar <名字>` 展示逐项理由

### 修复

- 列表视图按六项单项分数排序无效（分数嵌套于 `scores` 对象，比较器改为读取 `scores[维度]`）
- 图标模式文字溢出卡片（名称/备注改用 line-clamp 截断，`min-width: 0` 修复弹性布局）

### 优化

- WebUI 视觉重设计：渐变彩色头像（按姓名哈希取色）、玻璃拟态卡片与悬浮动效、分数胶囊、斑马纹表格、搜索框图标、按钮/滑条/模态框统一美化、雷达图渐变填充与顶点圆点

## [v0.1.3] - 2026-08-04

### 修复

- 解决「LLM 提示写入成功但 WebUI 无数据」：读取数据时按文件修改时间自动同步磁盘，WebUI 与 AI 工具即使分属不同插件实例也能看到彼此写入的数据
- `set_radar_scores` 工具写入容错：六项评分改为可选，缺省项保留原值（新建时默认 60），不再因缺参报错；成功写入时输出日志便于排查

## [v0.1.2] - 2026-08-04

### 修复

- 修复 WebUI 弹窗/视图隐藏失效问题：CSS 中 `.modal-mask`、`.cards` 等 `display` 样式覆盖了 HTML `hidden` 属性，导致编辑弹窗常驻无法关闭（新增全局 `[hidden] { display: none !important; }` 规则）

## [v0.1.1] - 2026-08-04

### 修复

- 修复 AI 工具类 dataclass 定义报错「non-default argument 'store' follows default argument」（`store` 字段增加默认值）

## [v0.1.0] - 2026-08-04

### 新增

- WebUI Page：卡片/列表双视图，支持按名称拼音、六项单项、综合分排序
- 搜索：姓名、全拼、拼音首字母、同音模糊匹配（基于 pypinyin）
- 数据双向修改：WebUI 增删改；AI 工具读写/新建
- AI 工具：`get_radar_scores`（只读查询）、`set_radar_scores`（新建/更新）
- Skill：`radar-eval`，说明六项口径、综合分公式与「AI 无权删除」约束
- 聊天指令：`/radar <名字>`、`/radar list`
- 配置项：`password_enabled` / `password`（WebUI 密码门禁，仅保护 WebUI）
- 六维能力（学习能力、心理承受力、社交能力、判断决策、自我认知、长期方向感），综合分按权重公式统一计算
- 数据持久化至 `data/plugin_data/astrbot_plugin_hexaradar/scores.json`
- 亮/暗主题跟随 WebUI
